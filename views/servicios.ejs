<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" integrity="sha512-Fo3rlrZj/k7ujTnHqz5M5Q1e5PZGF2kcpPRwNkA6Dng/jlXGrxjStbA18Fs6S47Rh/jvTwVjUBPz1o7pH5Pt4Q==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
  <!-- Menú de navegación -->
  <nav class="navbar">
    <div class="logo">
      <img src="/images/logo.png" alt="Hotel Paraíso Logo">
    </div>
    <ul class="nav-links">
      <li><a href="/">Inicio</a></li>
      <li><a href="/servicios">Servicios</a></li>
      <li><a href="#sobre-nosotros">Sobre Nosotros</a></li>
    </ul>
    <div class="burger">
      <i class="fas fa-bars"></i>
    </div>
  </nav>

  <!-- Encabezado de Servicios -->
  <header class="hero">
    <div class="hero-text">
      <h1><%= title %></h1>
      <p>Conoce más sobre lo que ofrecemos para hacer tu estadía inolvidable.</p>
    </div>
  </header>

  <!-- Carrusel -->
  <section class="carousel">
    <div class="carousel-container">
      <% services.forEach((service, index) => { %>
        <div class="slide <%= index === 0 ? 'active' : '' %>">
          <img src="<%= service.image %>" alt="<%= service.name %>">
          <div class="caption"><%= service.name %></div>
        </div>
      <% }) %>
      <a class="prev" onclick="plusSlides(-1)">&#10094;</a>
      <a class="next" onclick="plusSlides(1)">&#10095;</a>
    </div>
    <div class="dots-container">
		<% services.forEach((_, index) => { %>
			<span class="dot" onclick="currentSlide(<%= index + 1 %>)"></span>
		<% }) %>
	</div>

  </section>

  <!-- Listado de servicios -->
  <section class="services-list">
    <h2>Servicios disponibles</h2>
    <ul>
      <% services.forEach((service, index) => { %>
        <li class="service-card">
          <img src="<%= service.image %>" alt="<%= service.name %>">
          <h3><%= service.name %></h3>
          <p>Disfruta de nuestro <%= service.name.toLowerCase() %>, diseñado para tu comodidad.</p>
          <button onclick="openModal(<%= index %>)" class="btn">Más Información</button>
        </li>
      <% }) %>
    </ul>
  </section>

  <!-- Modal -->
  <div id="serviceModal" class="modal">
    <div class="modal-content">
      <span class="close" onclick="closeModal()">&times;</span>
      <img id="modal-image" src="" alt="Service Image">
      <h2 id="modal-title"></h2>
      <p id="modal-description"></p>
    </div>
  </div>

  <script src="/javascripts/carousel.js"></script>
  <script src="/javascripts/servicios.js"></script>
</body>
</html>

<!---- Footer ----->
<footer id="sobre-nosotros">
  <div class="footer-content">
    <!-- Columna 1: Sobre Nosotros -->
    <div class="footer-section">
      <h3>Sobre Nosotros</h3>
      <p>Teléfono: <%= phone %></p>
      <p>Ubicación: <%= location %></p>
      <p>Email: contacto@hotelparaiso.com</p>
    </div>
    <!-- Columna 2: Síguenos -->
    <div class="footer-section">
      <h3>Síguenos</h3>
      <a href="https://www.instagram.com/hotelparaiso_inventado" target="_blank" class="social-icon">
        <i class="fab fa-instagram"></i> @hotelParaiso
      </a><br>
      <a href="https://www.facebook.com/hotelparaiso_inventado" target="_blank" class="social-icon">
        <i class="fab fa-facebook"></i> @hotelParaiso
      </a>
    </div>
  </div>
  <div class="footer-bottom">
    <p>&copy; 2024 Hotel Paraíso. Todos los derechos reservados.</p>
  </div>
</footer>




